# ==================================
# Variables de Entorno - Docker
# ==================================
# Este archivo es usado por Docker Compose
# NO edites esto si estás usando docker-compose.yml

# Base de datos MySQL (contenedor Docker)
DB_HOST=db
DB_PORT=3306
DB_USER=acueducto
DB_PASSWORD=acueducto123
DB_NAME=control_vehicular

# Puerto del servidor
PORT=3000

# JWT Secret
JWT_SECRET=my_super_secret_jwt_key_2026

# Entorno
NODE_ENV=development
